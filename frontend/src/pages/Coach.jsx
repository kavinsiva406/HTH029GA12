import {useState} from "react"; import API from "../api";
export default function Coach(){const[q,setQ]=useState(""),[answer,setAnswer]=useState(""),[sources,setSources]=useState([]),[loading,setLoading]=useState(false);async function ask(){if(!q.trim())return;setLoading(true);try{const r=await API.post("/coach",{question:q});setAnswer(r.data.answer);setSources(r.data.sources||[])}catch{setAnswer("Unable to connect to AI.")}finally{setLoading(false)}}return <div className="coach"><p className="eyebrow">YOUR AI ASSISTANT</p><h1>Ask OnboardAI 🤖</h1><p>Ask questions about your company's onboarding material.</p><div className="chat-input"><textarea placeholder="Example: What should I do if I receive a phishing email?" value={q} onChange={e=>setQ(e.target.value)}/><button className="primary" onClick={ask}>{loading?"Thinking...":"Ask AI →"}</button></div>{answer&&<div className="ai-answer"><div className="ai-title">🤖 OnboardAI</div><p>{answer}</p>{sources.length>0&&<><h4>Sources</h4>{sources.map((s,i)=><div className="citation" key={i}>📄 {s.document} — Page {s.page}</div>)}</>}</div>}</div>}
