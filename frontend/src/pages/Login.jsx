import {useState} from "react"; import API from "../api";
export default function Login({onLogin}){const[email,setEmail]=useState(""),[password,setPassword]=useState(""),[error,setError]=useState("");async function login(){try{const r=await API.post("/login",null,{params:{email,password}});if(r.data.success)onLogin(r.data.user);else setError(r.data.message)}catch{setError("Backend is not running.")}}return <div className="login-page"><div className="login-card"><div className="logo">AI</div><h1>OnboardAI</h1><p>Your intelligent onboarding coach</p><input type="email" placeholder="Email" value={email} onChange={e=>setEmail(e.target.value)}/><input type="password" placeholder="Password" value={password} onChange={e=>setPassword(e.target.value)}/>{error&&<div className="error">{error}</div>}<button className="primary full" onClick={login}>Start Learning →</button><small>Demo: kavin@demo.com / 123456</small></div></div>}
