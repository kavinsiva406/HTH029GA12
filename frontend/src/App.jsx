import {useState} from "react";
import Login from "./pages/Login"; import Dashboard from "./pages/Dashboard"; import Learn from "./pages/Learn"; import Quiz from "./pages/Quiz"; import Coach from "./pages/Coach"; import Admin from "./pages/Admin";
export default function App(){const[user,setUser]=useState(null),[page,setPage]=useState("dashboard"),[selectedTopic,setSelectedTopic]=useState(null);if(!user)return <Login onLogin={setUser}/>;return <div className="app"><aside className="sidebar"><h2>OnboardAI</h2><button onClick={()=>setPage("dashboard")}>🏠 Dashboard</button><button onClick={()=>setPage("learn")}>📚 Learning</button><button onClick={()=>setPage("quiz")}>📝 Quiz</button><button onClick={()=>setPage("coach")}>🤖 AI Coach</button><button onClick={()=>setPage("admin")}>📄 Documents</button><div className="user-box">👤 {user.name}</div></aside><main className="content">{page==="dashboard"&&<Dashboard user={user} onLearn={t=>{setSelectedTopic(t);setPage("learn")}}/>}{page==="learn"&&<Learn topic={selectedTopic}/>} {page==="quiz"&&<Quiz user={user}/>} {page==="coach"&&<Coach/>} {page==="admin"&&<Admin/>}</main></div>}
